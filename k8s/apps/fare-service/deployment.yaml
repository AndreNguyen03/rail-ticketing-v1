apiVersion: apps/v1
kind: Deployment
metadata:
  name: fare-service
  namespace: rail-ticketing
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fare-service
  template:
    metadata:
      labels:
        app: fare-service
    spec:
      initContainers:
        - name: wait-for-schedule
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z schedule-service 8081; do sleep 2; done"]
      containers:
        - name: fare-service
          image: rail-ticketing-fare-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8087
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: OTEL_SERVICE_NAME
              value: fare-service
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8087
            initialDelaySeconds: 20
            periodSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8087
            initialDelaySeconds: 60
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
